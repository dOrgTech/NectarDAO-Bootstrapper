// TODO: this is used in both the "Lock NEC" and "Bid GEN" views
/*
<Progress percentage={100 * (1 - timeLeft / totalTime)} />
<Text>Current Period: ${period} of ${maxPeriods}</Text>
<Text>Next starts in ${format(timeLeft)}</Text>
*/
